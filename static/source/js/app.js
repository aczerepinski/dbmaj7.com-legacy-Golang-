import React, {Component} from 'react';
import ReactDOM from 'react-dom';

class App extends Component {
    render() {
        return (
        <div>
            this is the app component
        </div>

            );
    }
}

ReactDOM.render(<App/>, document.querySelector('#dbmaj7'));